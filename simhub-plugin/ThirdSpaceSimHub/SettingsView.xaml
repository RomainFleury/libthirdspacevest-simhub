<UserControl x:Class="ThirdSpaceSimHub.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="500"
             Background="#1E1E1E">
    
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#4FC3F7"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,15,0,10"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>
        <Style TargetType="Slider">
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Width" Value="200"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#424242"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#616161"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            
            <!-- Header -->
            <TextBlock Text="Third Space Vest" FontSize="24" FontWeight="Bold" Foreground="#4FC3F7" Margin="0,0,0,5"/>
            <TextBlock Text="Haptic feedback for sim racing" Foreground="#9E9E9E" Margin="0,0,0,20"/>
            
            <!-- Connection Status -->
            <Border Background="#2D2D2D" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Style="{StaticResource HeaderStyle}" Text="🔌 Connection" Margin="0,0,0,10"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="Status: " VerticalAlignment="Center"/>
                        <Ellipse x:Name="StatusIndicator" Width="10" Height="10" Margin="5,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="StatusText" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,5">
                        <TextBlock Text="Daemon Host:" VerticalAlignment="Center" Width="100"/>
                        <TextBox x:Name="HostTextBox" Width="150" Background="#424242" Foreground="#E0E0E0" 
                                 BorderBrush="#616161" Padding="5,3"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="Daemon Port:" VerticalAlignment="Center" Width="100"/>
                        <TextBox x:Name="PortTextBox" Width="80" Background="#424242" Foreground="#E0E0E0" 
                                 BorderBrush="#616161" Padding="5,3"/>
                    </StackPanel>
                    
                    <CheckBox x:Name="AutoConnectCheckBox" Content="Auto-connect on startup" Margin="0,10,0,5"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Button x:Name="ConnectButton" Content="Connect" Click="ConnectButton_Click"/>
                        <Button x:Name="DisconnectButton" Content="Disconnect" Click="DisconnectButton_Click"/>
                        <Button x:Name="TestButton" Content="Test Haptics" Click="TestButton_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- Effect Toggles -->
            <Border Background="#2D2D2D" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Style="{StaticResource HeaderStyle}" Text="⚡ Effects" Margin="0,0,0,10"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        
                        <CheckBox x:Name="BrakingCheckBox" Content="Braking" Grid.Row="0" Grid.Column="0"/>
                        <CheckBox x:Name="AccelerationCheckBox" Content="Acceleration" Grid.Row="0" Grid.Column="1"/>
                        <CheckBox x:Name="GForceCheckBox" Content="G-Force (Cornering)" Grid.Row="1" Grid.Column="0"/>
                        <CheckBox x:Name="GearShiftCheckBox" Content="Gear Shift" Grid.Row="1" Grid.Column="1"/>
                        <CheckBox x:Name="ImpactCheckBox" Content="Impact/Collision" Grid.Row="2" Grid.Column="0"/>
                        <CheckBox x:Name="RumbleCheckBox" Content="Surface Rumble" Grid.Row="2" Grid.Column="1"/>
                        <CheckBox x:Name="ABSCheckBox" Content="ABS Active" Grid.Row="3" Grid.Column="0"/>
                        <CheckBox x:Name="TCCheckBox" Content="Traction Control" Grid.Row="3" Grid.Column="1"/>
                    </Grid>
                </StackPanel>
            </Border>
            
            <!-- Intensity Sliders -->
            <Border Background="#2D2D2D" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Style="{StaticResource HeaderStyle}" Text="📊 Intensity" Margin="0,0,0,10"/>
                    
                    <!-- Braking -->
                    <StackPanel Margin="0,5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Braking:" Width="120"/>
                            <TextBlock x:Name="BrakingValueText" Width="40"/>
                        </StackPanel>
                        <Slider x:Name="BrakingSlider" Minimum="0" Maximum="2" 
                                TickFrequency="0.1" IsSnapToTickEnabled="True"
                                ValueChanged="Slider_ValueChanged"/>
                    </StackPanel>
                    
                    <!-- Acceleration -->
                    <StackPanel Margin="0,5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Acceleration:" Width="120"/>
                            <TextBlock x:Name="AccelerationValueText" Width="40"/>
                        </StackPanel>
                        <Slider x:Name="AccelerationSlider" Minimum="0" Maximum="2" 
                                TickFrequency="0.1" IsSnapToTickEnabled="True"
                                ValueChanged="Slider_ValueChanged"/>
                    </StackPanel>
                    
                    <!-- G-Force -->
                    <StackPanel Margin="0,5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="G-Force:" Width="120"/>
                            <TextBlock x:Name="GForceValueText" Width="40"/>
                        </StackPanel>
                        <Slider x:Name="GForceSlider" Minimum="0" Maximum="2" 
                                TickFrequency="0.1" IsSnapToTickEnabled="True"
                                ValueChanged="Slider_ValueChanged"/>
                    </StackPanel>
                    
                    <!-- Impact -->
                    <StackPanel Margin="0,5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Impact:" Width="120"/>
                            <TextBlock x:Name="ImpactValueText" Width="40"/>
                        </StackPanel>
                        <Slider x:Name="ImpactSlider" Minimum="0" Maximum="2" 
                                TickFrequency="0.1" IsSnapToTickEnabled="True"
                                ValueChanged="Slider_ValueChanged"/>
                    </StackPanel>
                    
                    <!-- Rumble -->
                    <StackPanel Margin="0,5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Rumble:" Width="120"/>
                            <TextBlock x:Name="RumbleValueText" Width="40"/>
                        </StackPanel>
                        <Slider x:Name="RumbleSlider" Minimum="0" Maximum="2" 
                                TickFrequency="0.1" IsSnapToTickEnabled="True"
                                ValueChanged="Slider_ValueChanged"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- Vest Layout Diagram -->
            <Border Background="#2D2D2D" CornerRadius="8" Padding="15">
                <StackPanel>
                    <TextBlock Style="{StaticResource HeaderStyle}" Text="🦺 Vest Layout" Margin="0,0,0,10"/>
                    <TextBlock Text="Front                 Back" FontFamily="Consolas" Margin="20,0"/>
                    <TextBlock Text="┌───┬───┐         ┌───┬───┐" FontFamily="Consolas"/>
                    <TextBlock Text="│ 0 │ 1 │ Upper   │ 4 │ 5 │" FontFamily="Consolas"/>
                    <TextBlock Text="├───┼───┤         ├───┼───┤" FontFamily="Consolas"/>
                    <TextBlock Text="│ 2 │ 3 │ Lower   │ 6 │ 7 │" FontFamily="Consolas"/>
                    <TextBlock Text="└───┴───┘         └───┴───┘" FontFamily="Consolas"/>
                    <TextBlock Text="" Margin="0,10,0,5"/>
                    <TextBlock Text="Braking → Front (0,1,2,3)" Foreground="#81C784" FontSize="11"/>
                    <TextBlock Text="Acceleration → Back (4,5,6,7)" Foreground="#64B5F6" FontSize="11"/>
                    <TextBlock Text="Left turn → Left side (0,2,4,6)" Foreground="#FFB74D" FontSize="11"/>
                    <TextBlock Text="Right turn → Right side (1,3,5,7)" Foreground="#FFB74D" FontSize="11"/>
                </StackPanel>
            </Border>
            
            <!-- Info -->
            <TextBlock Text="Requires Python daemon running on TCP port 5050" 
                       Foreground="#757575" FontSize="10" Margin="0,15,0,0" TextAlignment="Center"/>
            
        </StackPanel>
    </ScrollViewer>
</UserControl>

